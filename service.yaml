apiVersion: v1
kind: Service
metadata:
  name: new-docker-app
  namespace: acr-kube
spec:
  selector:
    app: new-docker-app
  type : LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
